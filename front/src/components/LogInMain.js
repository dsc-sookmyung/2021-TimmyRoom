import Center from './Center';
import Button from './Button';

const LogInMain = () => {
    return (
        <Center>
            <Button>로그인이</Button>
            <Button>된 상태</Button>
        </Center>
    );
};

export default LogInMain;
