import Center from './Center';
import Button from './Button';

const LogInPage = () => {
    return (
        <Center>
            <Button>로그인하는페이지</Button>
        </Center>
    );
};

export default LogInPage;
